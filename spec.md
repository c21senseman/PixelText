# PixelText 동작 규격

이 문서는 사용자에게 보이는 동작을 정의한다. 제품 원칙은 [ux.md](./ux.md)를 따른다.

## 1. 문서와 좌표

- 문서는 정수 좌표 `(x, y)`의 셀로 이루어진다. 오른쪽은 `x + 1`, 아래쪽은 `y + 1`이다.
- 좌표는 음수를 허용한다. 원점은 별도로 표시하지 않는다.
- 사용자는 빈 셀에도 커서를 놓고 입력할 수 있다.
- 셀 하나에는 사용자 기준 문자 하나가 들어간다. 한글, 결합 문자, 이모지 문자소 묶음은 각각 한 셀이다.
- 입력에서는 줄바꿈만 허용한다. 탭 등 나머지 C0 제어 문자는 거부한다. `CRLF`와 `CR`은 `LF`로 바꾼다.
- 좌표는 `-9,007,199,254,740,991`부터 `9,007,199,254,740,991`까지다. 결과가 범위를 벗어나는 동작은 적용하지 않고 오류를 표시한다.

### 공백과 글 구간

- Space는 문자를 만들지 않고 빈 셀 한 칸을 만든다.
- 좌우에 문자가 있는 정확히 한 칸의 빈 셀은 논리적 공백이다. 문자 수, 검색, 삽입, 삭제에서 글의 일부로 취급한다.
- 두 칸 이상의 빈 셀은 글 구간의 경계다. 경계 너머의 글은 앞 구간 편집 때문에 밀리거나 당겨지지 않는다.
- 이 문서에서 `줄`은 같은 `y`에서 논리적 공백을 포함해 이어지는 최대 글 구간이다.
- `문자 블록`은 한 줄에서 시작해 문자와 논리적 공백이 가로·세로·대각선으로 닿아 있는 범위다.

## 2. 화면 구성

- 상단에는 브랜드, 실행 취소, 다시 실행, 찾기, 책갈피, 파일, 도움말을 둔다.
- 중앙은 캔버스다. 밝은 종이색 배경에 어두운 고정폭 문자를 표시하며 격자선은 그리지 않는다.
- 넓은 글리프도 자기 셀 밖으로 그리지 않는다.
- 선택은 반투명 강조와 경계 손잡이로 표시한다. 커서는 파란 세로선이다.
- 오른쪽 아래에는 배율 조절과 미니맵을 둔다.
- 하단에는 커서 `x`, `y`, 화면 배율, 문자 수, 선택 크기, 저장 상태를 표시한다. 선택이 없으면 선택 크기를 숨긴다.
- 문자 수에는 실제 문자와 논리적 공백을 포함한다.
- 한 번에 찾기, 책갈피, 파일, 도움말 패널 중 하나만 연다.
- `Escape`, `Ctrl/Cmd + F`, `Ctrl/Cmd + Z`, `Ctrl/Cmd + Shift + Z`는 캔버스, 패널, 입력란 중 어디에 포커스가 있어도 동작한다.
- 폭 720px 이하에서는 상단의 실행 취소·다시 실행, 도구 이름, 하단의 배율과 저장 문구를 숨긴다. 기능 버튼, 저장 상태 점, 좌표, 문자 수, 선택 크기는 유지한다. 미니맵과 패널은 작게 배치한다.

### 시각 기준

- 앱은 전체 화면을 쓰며 상단 56px, 하단 32px다. 좁은 화면의 상단은 52px다.
- 100% 배율의 셀은 `20×24px`, 글자는 `19px`다.
- 캔버스는 `#f7f7f4`, 본문 문자는 `#252521`, 주요 강조는 `#5261e6`을 기준으로 한다.
- 보조 패널은 반투명한 흰색 카드다. 저장 완료는 녹색, 대기·진행은 주황색, 실패는 붉은색으로 구분한다.
- 사용자가 글꼴, 문자색, 꾸밈을 바꾸는 기능은 제공하지 않는다.

### 시작과 알림

- 저장 문서를 불러오는 동안 캔버스를 가리고 `캔버스를 준비하고 있습니다`를 표시한다.
- 문자가 하나도 없으면 `어디서든, 바로 쓰세요.`와 클릭, 끌기, 화면 이동, 확대 안내를 표시한다. 문자가 생기면 숨긴다.
- 오류와 완료 알림은 캔버스 아래쪽에 표시하고 약 4.2초 뒤 숨긴다.
- 버튼과 입력란에는 키보드 포커스 표시와 접근성 이름을 제공한다.
- 동작 축소 환경에서는 불필요한 애니메이션을 사실상 제거한다.

## 3. 화면 이동과 확대

- 마우스 오른쪽 버튼으로 캔버스를 끌면 화면을 이동한다. 기본 컨텍스트 메뉴는 열지 않는다.
- 캔버스에서 `Ctrl/Cmd + 휠`을 쓰면 포인터가 가리키던 논리 위치를 유지하며 확대·축소한다.
- `−`, `+` 버튼은 각각 현재 배율에 `0.8`, `1.25`를 곱한다.
- 배율 범위는 5%부터 400%다. 문자와 셀을 함께 확대한다.
- 저장 문서를 다시 열면 마지막 화면 중심과 배율을 복원한다.

## 4. 커서와 입력

- 캔버스를 클릭하면 해당 셀의 왼쪽 경계에 커서를 놓고 기존 선택을 해제한다.
- 선택이 없을 때 방향키는 커서를 해당 방향으로 한 셀씩 이동한다.
- 선택이 있을 때 방향키는 선택 영역과 그 안의 문자를 해당 방향으로 한 셀씩 이동한다.
- 입력, 클릭, 커서 명령 뒤에는 문자 입력 포커스를 유지한다.
- 커서 `(x, y)`는 `(x - 1, y)`와 `(x, y)` 사이의 삽입 위치다.
- 삽입 모드의 커서는 세로선이며, 덮어쓰기 모드의 커서는 현재 셀 전체를 표시한다.

### 문자 입력

- 기본값은 삽입 모드다. `Insert`를 누를 때마다 삽입과 덮어쓰기 모드를 전환하며, 커서·선택 영역 이동과 붙여넣기 뒤에도 현재 모드를 유지한다.
- 삽입 모드는 커서부터 같은 글 구간의 끝까지 한 칸 오른쪽으로 밀고 문자를 넣는다.
- 덮어쓰기 모드는 현재 셀의 문자를 밀지 않고 새 문자로 바꾸며, Space는 현재 셀을 비운다.
- 자동 대문자화, 자동 교정, 맞춤법 검사는 사용하지 않는다.
- 한 글자 입력 뒤 커서는 한 칸 오른쪽으로 간다.
- Space를 누르면 커서가 항상 한 칸 오른쪽으로 간다.
  - 현재 위치가 문자면 같은 글 구간을 한 칸 밀고 빈 셀을 넣는다.
  - 현재 위치가 논리적 공백이면 문서를 바꾸지 않고 그 칸을 지난다.
  - 그 밖의 빈 셀에서도 문서를 바꾸지 않고 이동한다.
- Space를 연속 입력하면 입력한 수만큼 거리가 생긴다.
- 선택 중 입력하면 선택 안의 문자를 지우고 왼쪽 위에서 입력한다. 삭제와 입력은 한 작업이다.

### 한글·IME 입력

- 조합 중인 문자열은 커서부터 미리보기로 표시한다. 확정 전에는 문서, 문자 수, 검색, 저장, 실행 취소 기록에 넣지 않는다.
- 확정 문자열은 문자소 묶음으로 나눠 한 번만 삽입한다.
- 확정 문자열은 현재 삽입·덮어쓰기 모드의 입력 규칙을 따른다.
- 조합 중 `Escape`는 조합을 취소한다.
- 조합 중 클릭, 커서 이동, 편집 명령을 실행하면 현재 조합을 먼저 확정한 뒤 명령을 실행한다.

### 여러 줄 입력과 붙여넣기

- 줄바꿈은 다음 `y`로 이동한다. 모든 줄은 처음 입력한 `x`에서 시작한다.
- 각 줄은 기본 입력 규칙으로 독립적으로 삽입한다. 기존 아래쪽 문자 블록은 내리지 않는다.
- 덮어쓰기 모드에서는 각 줄의 붙여넣기 범위만 교체하고 그 오른쪽 문자는 밀지 않는다.
- 빈 줄은 문서를 바꾸지 않지만 다음 줄의 `y`는 증가한다.
- 공백은 각 칸의 거리로 반영한다.
- 전체 입력 또는 붙여넣기는 한 번에 실행 취소한다.
- 완료 후 커서는 마지막 줄의 마지막 삽입 위치에 놓인다.

## 5. Enter와 지우기

### Enter

- 커서가 문자·논리적 공백 위에 있거나 바로 왼쪽이 글이면 해당 줄을 커서에서 나눈다.
- 커서 오른쪽 접미부를 다음 행의 기존 줄 시작 `x`부터 옮긴다. 커서는 옮긴 접미부 앞에 놓는다.
- 같은 문자 블록에서 현재 줄보다 아래에 있는 내용은 한 행 아래로 민다. 떨어진 내용은 움직이지 않는다.
- 현재 줄이 없으면 문서를 바꾸지 않고 커서만 같은 `x`의 다음 행으로 옮긴다.
- 선택 중이면 선택을 먼저 지우고 왼쪽 위에서 Enter를 적용한다.
- 옮길 내용이 블록 밖의 문자와 충돌하면 아무것도 바꾸지 않고 오류를 표시한다.

### Backspace

- 보통은 커서 왼쪽 한 칸을 없애고 같은 글 구간의 뒤 내용을 당긴 뒤 커서를 왼쪽으로 옮긴다.
- 왼쪽이 빈 셀이고 현재 위치에서 글이 시작하면 현재 글 구간을 한 칸 왼쪽으로 당겨 간격을 줄인다.
- 커서가 줄 시작이고 바로 윗행 같은 `x`에서 다른 줄이 시작하면 두 줄을 잇는다.
  - 현재 줄이 있으면 윗줄 끝에 붙이고, 연결된 아래 문자 블록을 한 행 올린다.
  - 현재 줄이 없으면 문서를 바꾸지 않고 윗줄 끝으로만 이동한다.
  - 다른 문자와 충돌하면 전체 동작을 취소한다.
- 선택 중이면 선택 안을 지우고 커서를 왼쪽 위에 둔다. 각 행에서 선택 오른쪽 경계에 바로 이어진 글만 선택 너비만큼 왼쪽으로 당긴다.

### Delete

- 보통은 커서 위치 한 칸을 없애고 같은 글 구간의 뒤 내용을 왼쪽으로 당긴다. 커서는 움직이지 않는다.
- 선택 중이면 선택 안의 문자만 지우고 커서를 왼쪽 위에 둔다. 오른쪽 글은 당기지 않는다.

## 6. 선택, 복사, 이동

### 사각형 선택

- 왼쪽 버튼으로 빈 곳이나 선택 밖을 끌면 사각형을 선택한다.
- 선택 범위는 왼쪽·위 경계를 포함하고 오른쪽·아래 경계를 제외한다.
- 가로 범위는 시작 커서와 끝 커서 사이의 셀이다. 인접한 두 커서 위치를 끌면 한 셀을 고른다.
- 세로로만 끌면 한 열을 고르고 시작 행과 끝 행을 모두 포함한다.
- 빈 셀도 선택할 수 있다.
- 포인터가 화면 가장자리나 바깥에 있으면 화면이 그 방향으로 자동 이동하고 선택을 계속 늘린다. 포인터가 안쪽으로 돌아오거나 끌기를 끝내면 멈춘다.
- 클릭만 하면 선택하지 않고 그곳에 커서를 놓는다. `Escape`는 선택과 열린 패널을 닫는다.

### 경계 크기 조절

- 선택의 상하좌우 경계를 끌 수 있다. 최소 크기는 `1×1`이다.
- 위·아래 경계는 선택 범위만 바꾼다. 문자를 움직이지 않는다.
- 왼쪽·오른쪽 경계를 넓히면 범위만 넓힌다.
- 왼쪽·오른쪽 경계를 줄이면 각 기존 행을 독립된 줄로 유지하며 넘친 내용을 다음 행으로 자동 줄바꿈한다.
  - 기존 줄 시작이 새 가로 범위 안이면 줄바꿈된 행도 그 `x`에서 시작한다. 아니면 새 왼쪽 경계에서 시작한다.
  - 빈 행은 한 행으로 유지한다.
  - 문자 앞과 문자 사이 빈 셀은 유지하고, 마지막 문자 뒤 빈 셀은 버린다.
  - 결과 높이에 맞춰 아래 경계를 조정한다.
  - 대상 범위의 기존 문자는 덮어쓴다.
- 크기 조절 중에는 결과 범위를 미리 표시한다. 문자 재배치는 한 번에 실행 취소한다.

### 복사와 붙여넣기

- `Ctrl/Cmd + C`는 선택 사각형을 일반 텍스트로 복사한다.
- 빈 셀은 공백, 행 경계는 `LF`가 된다. 선택 가장자리의 빈 셀도 복사 문자열에 포함한다.
- `Ctrl/Cmd + V`는 4절의 여러 줄 입력 규칙으로 붙여넣는다.
- 빈 셀은 저장되지 않으므로 빈 가장자리만 있는 배치는 텍스트 왕복으로 문서 경계를 보존하지 못한다.

### 영역 이동

- 선택 안을 왼쪽 버튼으로 끌면 사각형 전체를 옮긴다. 이동 중 원래 범위와 대상 범위를 미리 표시한다.
- 선택 중 방향키를 누르면 선택 영역과 그 안의 문자를 해당 방향으로 한 셀씩 옮긴다.
- 원래 범위는 비우고 대상 범위에 이동 전 내용을 놓는다.
- 삽입 모드에서 최초 목표 영역에 기존 문자가 있으면 목표와 겹친 문장 블록만 민다. 이동 경로의 다른 문장에는 영향을 주지 않는다.
- 마우스로 옮길 때는 목표 영역과 문장 블록의 실제 문자 경계 중심을 비교해 목표에서 블록 바깥쪽으로 민다. 가로·세로 차이가 모두 있으면 각 후보 중 문장 블록이 목표 영역에서 가장 짧게 벗어나는 한 방향을 사용하고, 두 중심이 완전히 같으면 상하좌우 전체에서 고른다. 대각선으로 밀지 않으며 거리가 같으면 가로 방향을 우선한다.
- 방향키로 옮길 때는 중심 위치와 관계없이 누른 방향키와 같은 방향으로만 문장 블록을 민다.
- 문장 블록이 밀리며 다른 문장 블록과 충돌하면 그 블록도 같은 방향으로 연쇄 이동한다.
- 삽입 모드라도 최초 목표 영역이 비어 있으면 기존 방식으로 바로 이동한다.
- 덮어쓰기 모드에서는 경로의 글을 밀지 않고 목표 영역의 기존 문자를 선택 내용으로 교체한다. 선택 안의 빈 셀도 목표 문자를 지운다.
- 붙여넣기는 영역 이동 규칙을 사용하지 않고 4절의 입력 모드를 그대로 따른다.
- 원본과 목표가 겹쳐도 이동 전 선택 내용을 기준으로 정확히 옮긴다.
- 클릭만 하고 이동하지 않으면 선택을 해제하고 해당 위치에 커서를 놓는다.
- 영역 복제와 `Alt + 끌기`는 제공하지 않는다.

## 7. 실행 취소와 다시 실행

- 상단 버튼과 `Ctrl/Cmd + Z`로 실행 취소한다.
- 상단 버튼과 `Ctrl/Cmd + Shift + Z`로 다시 실행한다.
- 입력, 붙여넣기, Enter, 줄 잇기, 선택 삭제·당김, 영역 이동, 가로 재배치는 각각 한 사용자 작업 단위다.
- 문서 내용과 함께 작업 전후의 커서와 선택을 복원한다.
- 실행 취소 뒤 새 편집을 하면 다시 실행 기록을 버린다.
- 최근 500개 작업을 보관한다. 앱을 다시 열거나 문서를 교체하면 기록을 지운다.

## 8. 찾기

- 상단 `찾기` 또는 `Ctrl/Cmd + F`로 찾기 패널을 열고 입력란에 포커스를 둔다.
- 입력과 동시에 전체 캔버스의 가로 글을 정확히 검색한다. 대소문자 변환, 정규식, Unicode 정규화는 하지 않는다.
- 논리적 공백 하나는 검색할 수 있다. 줄바꿈이나 연속 공백 둘 이상이 있는 검색어는 결과가 없다.
- 서로 다른 행이나 두 칸 이상 떨어진 글 사이를 걸쳐 일치하지 않는다.
- 결과는 위에서 아래, 같은 행에서는 왼쪽에서 오른쪽 순서다.
- `Enter`와 아래 버튼은 다음 결과, `Shift + Enter`와 위 버튼은 이전 결과로 간다. 끝에서는 순환한다.
- 이동하면 결과가 화면 가운데 오고 커서가 결과 시작에 놓인다. 모든 일치를 주황 경계로, 현재 일치를 더 진하게 표시한다.
- `Escape` 또는 닫기 버튼으로 패널을 닫는다. 문서 편집, 실행 취소, 다시 실행, 문서 교체 뒤에는 기존 결과를 비운다.
- 패널을 닫았다 다시 열면 지우지 않은 검색어를 유지한다.

## 9. 책갈피

- 책갈피 패널에서 현재 커서 좌표를 이름과 함께 저장한다.
- 이름을 비워 저장하면 `지점 N`을 사용한다. 공백뿐인 직접 이름은 허용하지 않는다.
- 목록에는 이름과 `x, y`를 표시한다.
- 책갈피를 누르면 패널을 닫고 커서와 화면 중심을 그 좌표로 옮긴다.
- 각 책갈피는 개별 삭제할 수 있다.

## 10. 미니맵

- 미니맵은 문자 배치를 점 또는 밀도로 표시하고 현재 화면 범위를 파란 사각형으로 표시한다. 글자 모양은 표시하지 않는다.
- 기본 범위는 모든 문자와 주변 여백, 현재 화면 범위를 함께 포함한다. 문서가 비었으면 현재 화면 범위다.
- 미니맵을 클릭하거나 끌면 그 위치를 화면 중심으로 삼는다.
- 미니맵 위 `Ctrl/Cmd + 휠`은 미니맵만 100%부터 1600%까지 확대·축소한다. 캔버스 배율은 바꾸지 않는다.
- 미니맵 배율은 문서를 다시 열 때 복원하지 않는다.

## 11. 저장과 문서 교체

### 자동 저장

- 브라우저 프로필마다 현재 문서 하나를 자동 저장한다.
- 문자, 카메라 위치·배율, 책갈피를 저장한다. 커서, 선택, 입력 모드, 검색, 실행 취소 기록, 미니맵 배율은 저장하지 않는다.
- 문자, 카메라, 책갈피가 바뀔 때만 저장을 예약한다. 커서, 선택, 입력 모드, 검색만 바뀌면 저장하지 않는다.
- 변경이 멈춘 뒤 약 500ms 후 저장한다. 페이지를 떠날 때도 저장을 시도한다.
- 상태는 `불러오는 중`, `저장 대기`, `저장 중`, `저장됨`, `저장 실패`로 구분한다.
- 실패하면 오류를 유지하고 `재시도` 버튼을 표시한다.

### 새로 만들기

- 문자나 책갈피가 있으면 현재 내용을 지울지 확인한다. 둘 다 없으면 바로 만든다.
- 문서, 책갈피, 선택, 검색어, 검색 결과, 실행 기록을 지운다.
- 커서와 화면 중심은 `(0, 0)`, 캔버스 배율은 100%, 미니맵 배율은 100%로 되돌린다.
- 완료 알림을 표시하고 새 상태를 자동 저장한다.

### JSON 가져오기·내보내기

- 파일 이름은 `pixeltext-YYYY-MM-DD.json`이다.
- JSON은 문자 좌표, 책갈피, 카메라를 보존한다. 형식은 다음과 같다.

```json
{
  "version": 1,
  "chunkSize": 64,
  "chunks": [{ "x": 0, "y": 0, "cells": [[0, "A"]] }],
  "bookmarks": [{ "id": "b1", "name": "예시", "x": 0, "y": 0 }],
  "camera": { "x": 0, "y": 0, "zoom": 1 }
}
```

- 청크 `(x, y)`는 `64×64` 셀 범위다. 셀 인덱스는 `0..4095`이며 `행 × 64 + 열`이다. 음수 좌표도 바닥 나눗셈으로 청크를 정한다.
- 내보낼 때 청크는 `(y, x)`, 셀은 인덱스 오름차순으로 정렬한다. 빈 셀과 빈 청크는 쓰지 않는다.
- 가져오기는 필수 필드, 버전 `1`, 청크 크기 `64`, 좌표·인덱스 범위, 중복 청크·셀, 제어 문자가 없는 단일 문자소 값, 중복·빈 책갈피 ID, 카메라 유효성을 검사한다. 알 수 없는 최상위 필드는 무시한다.
- 이전 파일의 실제 Space 셀은 빈 셀로 읽는다.
- 파일 전체가 유효할 때만 현재 문서, 책갈피, 카메라를 한 번에 교체한다. 실패하면 기존 상태를 유지하고 오류를 표시한다.
- 성공하면 커서를 `(0, 0)`에 놓고 선택, 검색 결과, 실행 기록을 지운다.

### TXT 내보내기

- 파일 이름은 `pixeltext-YYYY-MM-DD.txt`이다.
- 실제 문자가 있는 최소 경계 사각형을 일반 텍스트로 만든다. 빈 셀은 공백, 행 경계는 `LF`다.
- 경계 안의 행 끝 공백을 유지한다. 빈 문서는 빈 파일이 된다.
- 좌표, 책갈피, 카메라는 보존하지 않는다.

### 일반 텍스트 변환 한도

- 복사, 붙여넣기, 밀어내는 목표 영역, 가로 선택 재배치, TXT 내보내기는 너비 100,000셀, 높이 100,000셀, 전체 1,000,000셀을 넘을 수 없다.
- 한도를 넘으면 아무것도 바꾸지 않고 오류를 표시한다. TXT가 너무 크면 JSON 내보내기를 안내한다.

## 12. 도움말과 오류

- 도움말에는 화면 이동, 캔버스·미니맵 확대, 사각형 선택, 선택 크기 조절·이동, 실행 취소·다시 실행 조작을 표시한다.
- 편집 충돌, 좌표 초과, 금지 문자, 변환 한도, 잘못된 파일, 저장 실패는 사용자에게 원인을 알 수 있는 한국어 문구로 표시한다.
- 오류가 난 복합 동작은 일부만 적용하지 않는다.
