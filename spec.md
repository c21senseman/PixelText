# 무한 문자 캔버스 편집기 규격

## 1. 개요

무한한 2차원 격자 공간에 문자를 배치하는 웹 기반 텍스트 편집기다.

* 논리적 공간은 사용자 관점에서 경계가 없다. 단, 좌표는 JavaScript 안전 정수 범위를 사용한다.
* 좌표는 정수 `(x, y)`를 사용한다.
* 한 셀에는 사용자 기준 문자 하나만 들어간다.
* 빈 셀은 저장하지 않는다.
* 줄보다 좌표를 기본 자료 단위로 사용한다.
* 사용자는 빈 공간을 포함한 임의 위치에서 입력할 수 있다.

---

## 2. 기술 구성

* TypeScript
* Vite
* Canvas 2D
* IndexedDB
* 숨겨진 `<textarea>` 기반 문자 입력
* `Intl.Segmenter` 기반 문자 분리

문자, 편집 상태, 화면 그리기, 입력, 저장 기능을 별도 계층으로 분리한다.

---

## 3. 좌표

문자 위치는 픽셀이 아닌 셀 좌표로 저장한다.

```ts
type Position = {
  x: number;
  y: number;
};
```

문서의 셀 좌표는 항상 `Number.isSafeInteger()`를 만족해야 한다. 즉, 표현 가능한 범위는 `Number.MIN_SAFE_INTEGER`에서 `Number.MAX_SAFE_INTEGER`까지다.

입력, 이동, 가져오기 또는 편집 결과가 이 범위를 벗어나면 해당 동작 전체를 적용하지 않고 오류를 표시한다.

화면 좌표는 카메라를 기준으로 계산한다.

```text
논리 좌표
→ 카메라 상대 좌표
→ 확대율 적용
→ 화면 픽셀
```

음수 좌표를 허용한다.

카메라 좌표와 문서 좌표는 독립적이다.

청크 좌표와 내부 인덱스는 다음과 같이 계산한다.

```ts
const cx = Math.floor(x / 64);
const cy = Math.floor(y / 64);
const lx = x - cx * 64; // 0..63
const ly = y - cy * 64; // 0..63
const index = ly * 64 + lx; // 0..4095
```

---

## 4. 문자 저장

64×64 셀을 하나의 청크로 사용한다.

```ts
type ChunkKey = `${number},${number}`;

type Chunk = Map<number, string>;

type Document = Map<ChunkKey, Chunk>;
```

셀 내부 위치는 청크 내부 인덱스로 변환한다.

빈 셀은 저장하지 않는다. 빈 셀은 `null`로 표현한다.

공백 문자 `" "`(U+0020)는 실제 셀로 저장하지 않고 빈 셀로 취급한다. 좌우에 문자가 있는 정확히 한 칸의 빈 셀만 논리적 공백으로 해석하여 문자 수, 검색, 삽입과 삭제의 연속 구간 판정에 포함한다. 두 칸 이상의 연속된 빈 셀은 공백 문자열이 아니라 서로 떨어진 문자 구간의 경계다.

문자를 삭제해 청크가 비면 해당 청크도 제거한다.

음수 좌표에서도 동일한 청크 계산 결과가 나오도록 바닥 나눗셈을 사용한다.

---

## 5. 셀

셀 하나에는 문자소 묶음 하나를 저장한다.

예:

```text
A        → 1셀
한       → 1셀
😀       → 1셀
👨‍👩‍👧‍👦 → 1셀
```

문자 분리는 다음을 사용한다.

```ts
new Intl.Segmenter(undefined, {
  granularity: "grapheme"
});
```

입력과 붙여넣기의 줄바꿈은 셀 문자로 저장하지 않는다. `CRLF`와 `CR`은 먼저 `LF`로 정규화한다. 탭(U+0009)과 나머지 C0 제어 문자는 초기 버전에서 입력하거나 가져올 수 없다.

JSON으로 가져온 각 셀 값도 현재 런타임의 `Intl.Segmenter`로 분리했을 때 문자소 묶음 하나여야 한다.

셀 값은 초기에는 문자열만 저장한다.

```ts
type Cell = string;
```

저장 계층은 이후 색상 등의 속성을 추가할 수 있도록 추상화한다.

---

## 6. 글꼴과 표시

고정폭 글꼴을 사용한다.

사용자가 글꼴을 변경하는 기능은 제공하지 않는다.

격자선은 표시하지 않는다.

각 문자는 반드시 자신의 셀 안에 그린다. 폭이 큰 이모지 등의 글리프가 인접 셀 영역을 침범하지 않게 처리한다.

---

## 7. 화면 그리기

Canvas 2D를 사용한다.

전체 문서를 순회하지 않는다.

현재 화면과 겹치는 청크만 찾고, 해당 청크 안에서 실제 문자가 존재하는 셀만 그린다.

그리기 순서는 다음과 같다.

```text
문자
→ 선택 영역
→ 커서
→ 보조 표시
```

화면 변경이 발생했을 때만 다시 그리기를 요청한다.

```ts
needsRender = true;
```

실제 그리기는 `requestAnimationFrame()`에서 처리한다.

---

## 8. 카메라

카메라는 다음 상태를 가진다.

```ts
type Camera = {
  x: number;
  y: number;
  zoom: number;
};
```

`x`, `y`는 셀 단위의 유한한 부동소수점 값이고 `zoom`은 `0.05`에서 `4`까지다. 표시 배율로는 5%에서 400%까지다.

### 이동

`마우스 오른쪽 버튼 + 끌기`로 이동한다.

### 확대/축소

`Ctrl + 휠`을 사용한다.

마우스가 가리키는 논리 좌표가 가능한 한 같은 화면 위치에 유지되도록 확대한다.

문자와 셀 크기를 함께 확대한다.

문서를 다시 열면 마지막 카메라 위치와 확대율을 복원한다.

---

## 9. 커서

커서는 셀 좌표를 가진다.

```ts
type Cursor = {
  x: number;
  y: number;
};
```

빈 공간에도 존재할 수 있다.

방향키 동작:

```text
← : x - 1
→ : x + 1
↑ : y - 1
↓ : y + 1
```

캔버스를 클릭하면 해당 셀로 커서를 이동한다.

---

## 10. 문자 입력

실제 키보드 입력은 숨겨진 `<textarea>`에서 받는다.

한글 입력기 조합을 지원한다.

```text
compositionstart
compositionupdate
compositionend
```

조합 중인 문자는 확정 전까지 문서 자료에 저장하지 않는다.

한글 입력을 필수 지원한다.

### IME 처리 규칙

* 문자 입력은 `keydown`의 키 값을 문서에 직접 쓰지 않고, 숨겨진 `<textarea>`의 `beforeinput`, `input`, composition 이벤트를 입력 계층이 정규화해 처리한다.
* 조합 중인 문자열은 현재 커서에 미리보기로만 그린다. 문서, 검색, 문자 수, 자동 저장, 실행 취소 기록에는 포함하지 않는다.
* `compositionend`로 확정된 문자열은 문자소 묶음으로 나눈 뒤 왼쪽에서 오른쪽 순서로 한 번만 삽입한다. 브라우저가 `compositionend` 전후로 같은 `input` 데이터를 보내더라도 입력 계층은 중복 삽입을 방지해야 한다.
* 조합 중 `Escape`는 조합을 취소한다. 클릭이나 커서 이동 명령이 발생하면 브라우저의 조합 종료를 먼저 처리한 후 해당 명령을 적용한다.
* 실제 입력, 클릭 또는 커서 명령 후에는 숨겨진 `<textarea>`의 포커스를 유지한다.

### 기본 입력 방식

삽입 방식이다.

커서 `(x, y)`는 `x - 1`의 문자와 `x`의 문자 사이의 삽입 위치를 뜻한다. 현재 셀이 문자 구간에 포함되면 현재 위치부터 오른쪽으로 문자 구간이 끝날 때까지의 모든 문자를 한 칸씩 이동한 뒤 새 문자를 넣는다.

문자 사이의 한 칸짜리 빈 셀은 논리적 공백이므로 같은 문자 구간에 포함한다. 두 칸 이상의 빈 셀 뒤에 있는 별개 문자열은 이동시키지 않는다.

Space 입력은 항상 커서를 한 칸 이동시키되 셀을 저장하지 않는다. 현재 위치가 문자이면 오른쪽의 같은 문자 구간을 한 칸 밀어 빈 셀을 삽입하고, 이미 빈 셀이면 그 셀을 지나간다. 연속 Space 입력도 입력 횟수만큼 반영되므로 두 칸 이상의 빈 영역을 만들 수 있다. 이후 오른쪽에 문자가 입력되어 빈 셀의 좌우가 모두 문자가 되면 정확히 한 칸인 간격만 논리적 공백이 된다.

입력 완료 후 커서는 오른쪽으로 한 칸 이동한다.

---

## 11. Enter

Enter는 **현재 문자 줄을 커서에서 나누고 새 줄을 삽입**한다. 일반 텍스트 편집기의 Enter와 같이 새 줄의 문자 앞으로 커서가 이동한다.

### 용어

* **줄**: 같은 `y`에서 두 칸 이상의 빈 셀에 의해 구분되는 최대 연속 문자 구간이다. 문자 사이의 한 칸짜리 빈 셀은 논리적 공백으로 줄에 포함된다.
* **현재 줄**: 커서의 현재 셀이 문자 또는 논리적 공백이면 그 셀을 포함한 줄이다. 현재 셀이 문자 구간 밖이고 왼쪽 셀이 문자 또는 논리적 공백이면 왼쪽 셀을 포함한 줄을 사용한다. 둘 다 아니면 현재 줄이 없다.
* **문자 블록**: 현재 줄의 셀에서 시작해 상하좌우 및 대각선으로 인접한 문자 셀과 논리적 공백을 따라가며 얻는 8-방향 연결 요소다. 이 규칙은 공간상 멀리 떨어진 내용을 함께 이동시키지 않기 위한 기준이다.

### 동작

1. 현재 줄의 시작 `x`(`lineStart`)와 문자 블록을 편집 전 상태에서 계산한다.
2. 문자 블록 중 현재 줄보다 아래에 있는 모든 셀을 `y + 1`로 이동한다. 아래에서부터 위로 적용하여 덮어쓰기를 방지한다.
3. 현재 줄의 커서 `x` 이상에 있는 접미부를 순서를 유지하며 `(lineStart, y + 1)`부터 이동한다.
4. 커서를 `(lineStart, y + 1)`로 이동한다. 즉, 이동한 접미부의 첫 문자 앞에 놓인다.

이 이동으로 문자 블록 밖의 기존 셀과 충돌하면 Enter를 적용하지 않고 충돌 오류를 표시한다. 부분 적용은 하지 않는다.

현재 줄이 없으면 문서를 변경하지 않고 커서만 `(x, y + 1)`로 이동한다.

```text
abc|def
hello
```

↓

```text
abc
|def
hello
```

위 예시에서 `hello`는 같은 문자 블록이므로 한 줄 아래로 이동한다. 공간상 멀리 떨어진 독립적인 내용은 이동하지 않는다.

Enter 동작을 바꾸는 추가 단축키는 제공하지 않는다.

---

## 12. Backspace / Delete

삽입 편집 방식에 맞춰 동작한다.

### Backspace

* 커서 왼쪽에 현재 행의 문자 구간이 없고 바로 윗행의 같은 `x`에서 문자 구간이 시작하면, 문자를 변경하지 않고 커서를 그 윗줄 끝의 오른쪽으로 이동한다. 이 판단은 이동 기록이 아닌 현재 문서 배치만 사용한다.
* 커서 왼쪽 문자를 삭제한다.
* 같은 연속 문자열의 뒤 문자를 왼쪽으로 당긴다.
* 커서 왼쪽이 빈 셀이고 현재 위치가 문자이면, 현재 문자 구간을 한 칸 왼쪽으로 당겨 빈 셀 하나를 제거한다.
* 커서를 왼쪽으로 이동한다.

### Delete

* 현재 위치의 문자를 삭제한다.
* 같은 연속 문자열의 오른쪽 문자를 왼쪽으로 당긴다.

두 칸 이상의 빈 셀로 분리된 별개의 문자열은 이동하지 않는다. 문자 사이의 한 칸짜리 빈 셀은 연속 문자열의 일부다.

---

## 13. 선택

기본 선택 방식은 사각형 선택이다.

```ts
type Selection = {
  x1: number;
  y1: number;
  x2: number;
  y2: number;
};
```

마우스 끌기로 선택한다.

선택 중 포인터를 화면 가장자리에 가까이 가져가거나 화면 밖으로 끌면 해당 방향으로 카메라가 자동 이동하고, 이동한 논리 좌표까지 선택 영역이 계속 확장된다. 포인터가 가장자리에서 멀어지거나 선택을 끝내면 자동 이동을 멈춘다.

가로 선택 경계는 커서 배치와 같은 기준을 사용한다. 시작 위치와 현재 포인터가 가리키는 커서 위치 사이의 셀만 선택하며, 현재 포인터가 가리키는 셀을 추가로 포함하지 않는다. 따라서 인접한 커서 위치 사이를 끌면 정확히 한 셀이 선택된다. 세로로만 끌 때는 해당 `x`의 한 열과 시작·끝 행을 포함한다.

선택 범위에 빈 셀이 포함될 수 있다.

선택 사각형의 왼쪽 또는 오른쪽 세로 경계를 가로로 끌어 선택 폭을 조절할 수 있다. 폭을 줄이면 각 기존 행을 서로 독립된 줄로 유지하면서 오른쪽으로 넘치는 문자만 다음 행으로 보낸다. 빈 행도 하나의 기존 줄로 유지한다. 각 행의 마지막 문자 뒤 빈 셀은 흐름에서 제외하되, 문자 앞과 문자 사이의 빈 셀은 유지한다. 폭을 늘릴 때는 선택 경계만 넓히고 문자는 이동하거나 이전 행으로 당기지 않는다. 폭을 줄여 다시 배치한 내용의 높이에 맞춰 아래쪽 선택 경계가 자동으로 조정되며, 위·아래 가로 경계는 직접 조절하거나 문자 배치에 사용하지 않는다. 대상 영역의 기존 문자는 영역 이동과 마찬가지로 덮어쓴다. 전체 문자 변경은 하나의 실행 취소 작업이다.

`x1`, `y1`은 포함하고 `x2`, `y2`는 포함하지 않는 반열린 범위 `[x1, x2) × [y1, y2)`를 사용한다. 역방향으로 끌어도 저장할 때는 `x1 <= x2`, `y1 <= y2`가 되도록 정규화한다.

선택이 있는 상태에서 문자를 입력하거나 붙여넣으면 선택 범위의 모든 점유 셀을 삭제하고 왼쪽 위 `(x1, y1)`에 커서를 놓은 후 해당 동작을 수행한다. 삭제와 후속 입력은 하나의 실행 취소 작업이다.

선택이 있는 상태의 Backspace는 선택 범위의 점유 셀을 삭제하고 커서를 `(x1, y1)`로 이동한다. 각 행에서 선택 오른쪽 경계에 이어진 문자 구간은 선택 너비만큼 왼쪽으로 당긴다. 선택이 문자를 포함하거나 문자 구간의 가로 경계와 맞닿은 경우에만 해당 구간에 영향을 주며, 한 칸 이상 떨어진 별개 문자열은 움직이지 않는다. Delete는 당김 없이 선택 범위의 점유 셀만 삭제하고 커서를 `(x1, y1)`로 이동한다.

---

## 14. 복사와 붙여넣기

사각형 영역은 일반 텍스트로 복사한다.

```text
ABC
DEF
GHI
```

빈 셀은 공백 문자로 변환한다. 붙여넣을 때 공백은 다시 빈 셀로 변환하며 연속 공백도 각각 한 칸의 이동 또는 삽입으로 반영한다. 저장된 문자가 없는 선택 영역의 가장자리 빈 셀은 문서 경계로 남지 않으므로 일반 텍스트 왕복이 완전히 무손실인 것은 아니다.

붙여넣기의 줄바꿈은 `y + 1`로 해석한다.

각 줄은 붙여넣기를 시작한 동일한 `x` 좌표에서 시작한다.

붙여넣기는 **삽입 방식**이다. 각 줄의 문자소 묶음을 해당 줄의 시작 좌표에서 기본 문자 입력과 동일하게 왼쪽에서 오른쪽으로 순차 삽입한다. 따라서 대상 위치의 연속 문자열은 입력된 셀 수만큼 오른쪽으로 밀린다. 공백은 저장하지 않는 빈 셀로 삽입하며 연속 공백도 입력된 개수만큼 반영한다.

여러 줄을 붙여넣을 때 각 줄은 독립적으로 삽입하며 Enter 동작처럼 기존 아래쪽 문자 블록을 내리지 않는다. 빈 줄은 문서를 변경하지 않는다.

붙여넣기 전체는 하나의 실행 취소 작업이다.

---

## 15. 영역 이동

선택 영역을 마우스로 끌어 다른 좌표로 이동할 수 있다.

원래 위치는 비우고 대상 위치에 내용을 배치한다.

영역 이동은 이동 전 스냅샷을 기준으로 계산하므로 원본과 대상이 겹쳐도 안전하다. 선택 사각형의 빈 셀도 대상에서 빈 셀로 적용하며, 대상 영역의 기존 문자는 덮어쓴다. 전체 이동은 하나의 원자적 실행 취소 작업이다.

영역 복제 및 `Alt + 끌기` 복제는 지원하지 않는다.

---

## 16. 실행 취소

다음 단축키를 지원한다.

```text
Ctrl + Z       실행 취소
Ctrl + Shift+Z 다시 실행
```

변경 사항은 명령으로 저장한다.

```ts
type Change = {
  x: number;
  y: number;
  before: string | null;
  after: string | null;
};
```

붙여넣기, 영역 이동, Enter 등은 여러 변경을 하나의 `batch` 작업으로 묶는다. 한 batch에서 같은 좌표가 여러 번 변경되면 편집 전의 첫 `before`와 편집 후의 마지막 `after`만 남긴다.

실행 취소와 다시 실행은 문서 변경과 함께 해당 명령 전후의 커서와 선택 상태를 복원한다. 실행 취소 후 새 편집을 하면 다시 실행 스택을 비운다. 실행 취소 기록은 프로그램을 다시 열 때 복원하지 않는다.

---

## 17. 미니맵

미니맵을 제공한다.

개별 문자는 표시하지 않는다.

청크 또는 일정 영역별 문자 밀도를 표시한다.

현재 카메라가 보고 있는 영역을 미니맵 위에 표시한다.

미니맵 클릭 또는 끌기로 해당 위치로 이동할 수 있다.

미니맵의 전체 범위는 저장된 문자가 있는 청크의 경계 사각형과 현재 카메라 범위를 모두 포함하도록 계산한다. 가로와 세로는 같은 비율로 축소하고 남는 영역은 여백으로 둔다. 문서가 비어 있으면 현재 카메라 범위만 표시한다.

---

## 18. 책갈피

현재 좌표를 책갈피로 저장할 수 있다.

책갈피는 다음 정보를 가진다.

```ts
type Bookmark = {
  id: string;
  name: string;
  x: number;
  y: number;
};
```

책갈피를 선택하면 해당 위치로 카메라와 커서를 이동한다.

---

## 19. 찾기

기본 문자열 찾기를 제공한다.

* 문자열 찾기
* 다음 결과
* 이전 결과
* 전체 캔버스 검색

정규식 검색은 초기 버전에 포함하지 않는다.

검색 결과는 좌표 목록으로 관리한다.

검색은 각 `y`의 가로 방향 문자만 대상으로 한다. 두 칸 이상의 빈 셀을 만나면 문자열이 끝나며 서로 다른 행을 걸쳐 일치하지 않는다. 문자 사이의 한 칸짜리 빈 셀은 공백으로 검색할 수 있고, 두 칸 연속 공백을 포함한 검색어는 허용하지 않는다.

초기 버전은 Unicode 코드 포인트의 정확한 일치를 사용하며 대소문자 무시와 Unicode 정규화를 자동으로 수행하지 않는다. 결과는 `(y, x)` 오름차순으로 정렬하고 다음/이전 검색은 끝에서 순환한다.

---

## 20. 저장

자동 저장은 IndexedDB를 사용한다.

저장은 청크 단위로 수행한다.

변경된 청크만 다시 저장할 수 있게 구성한다.

다음 상태도 저장한다.

* 문자 청크
* 카메라 위치
* 확대율
* 책갈피

초기 버전은 브라우저 프로필당 하나의 현재 문서를 관리한다. JSON 가져오기는 검증에 성공한 후에만 현재 문서를 교체한다.

문서 변경 후 500ms 동안 추가 변경이 없으면 자동 저장한다. 더 최근의 변경이 발생하면 타이머를 다시 시작한다. 변경된 모든 청크와 메타데이터를 하나의 IndexedDB 읽기-쓰기 트랜잭션으로 저장한다. 청크 삭제도 같은 트랜잭션에 포함한다.

저장이 실패하면 변경된 청크의 dirty 상태를 유지하고 상태 표시줄에 오류를 표시한다. 사용자가 재시도할 수 있어야 하며 저장 실패를 성공으로 표시해서는 안 된다. `pagehide`에서 저장을 시도하되, 이 이벤트만을 내구성 보장으로 사용하지 않는다.

---

## 21. 파일 형식

기본 파일 형식은 청크 기반 JSON이다.

```json
{
  "version": 1,
  "chunkSize": 64,
  "chunks": [
    {
      "x": -1,
      "y": 0,
      "cells": [
        [63, "A"],
        [65, "B"]
      ]
    }
  ],
  "bookmarks": [
    {
      "id": "b1",
      "name": "예시",
      "x": -1,
      "y": 0
    }
  ],
  "camera": {
    "x": 0,
    "y": 0,
    "zoom": 1
  }
}
```

`chunks` 배열의 `x`, `y`는 청크 좌표다. `cells`의 각 항목은 `[내부 인덱스, 문자열]`이며 내부 인덱스는 `0..4095`의 정수다. 빈 청크는 내보내지 않는다.

내보내기 결과를 결정적으로 만들기 위해 청크는 `(y, x)`, 셀은 내부 인덱스 오름차순으로 정렬한다.

### JSON 가져오기 검증

* `version`, `chunkSize`, `chunks`, `bookmarks`, `camera`는 필수 필드다. version 1에서 알 수 없는 최상위 필드는 무시한다.
* `version`은 `1`, `chunkSize`는 `64`여야 한다. 알 수 없는 version은 가져오지 않는다.
* 청크 좌표, 복원된 셀 좌표, 책갈피 좌표는 모두 안전 정수여야 한다.
* 청크 좌표와 각 청크의 내부 인덱스는 중복될 수 없다.
* 셀 문자열은 비어 있지 않은 문자소 묶음 하나여야 하며 금지된 제어 문자를 포함할 수 없다. 이전 문서의 U+0020 셀은 호환성을 위해 읽되 빈 셀로 정규화한다.
* 책갈피 `id`는 비어 있지 않고 중복될 수 없다.
* 카메라 값은 유한한 수여야 하고 `zoom`은 허용 범위 안에 있어야 한다.
* 파일 전체를 임시 상태에서 검증한 후 한 번에 교체한다. 하나라도 잘못되면 현재 문서는 변경하지 않는다.

JSON 가져오기와 내보내기를 지원한다.

텍스트 파일 `.txt` 내보내기도 지원한다.

텍스트 내보내기는 사용된 셀 전체의 경계 상자를 계산하고 빈 셀을 공백으로 변환한다. 줄바꿈은 `LF`를 사용하고 경계 상자 안의 후행 공백을 유지한다.

### 텍스트 래스터 한도

일반 텍스트로 변환하는 복사, 붙여넣기, TXT 내보내기에는 다음 한도를 적용한다.

```ts
const MAX_TEXT_WIDTH = 100_000;
const MAX_TEXT_HEIGHT = 100_000;
const MAX_TEXT_CELLS = 1_000_000;
```

너비, 높이 또는 `너비 × 높이`가 한도를 넘으면 동작 전체를 수행하지 않고 오류를 표시한다. 곱셈 오버플로를 피하기 위해 `width > Math.floor(MAX_TEXT_CELLS / height)` 방식으로 검사한다. TXT 전체 경계가 한도를 넘으면 JSON 내보내기를 안내한다.

---

## 22. 원점

내부적으로 `(0, 0)` 원점을 사용한다.

원점은 특별한 화면 요소로 표시하지 않는다.

사용자는 음수 좌표를 포함해 어느 방향으로든 이동할 수 있다.

---

## 23. 상태 표시

화면 하단에 다음 정보를 표시한다.

```text
x: -42
y: 173
확대: 125%
문자: 124820
선택: 8×4
```

문자 수에는 저장된 문자와 문자 사이의 한 칸짜리 논리적 공백을 포함한다. 선택 영역이 없으면 선택 크기는 숨긴다.

---

## 24. 규모 목표

논리 좌표에는 JavaScript 안전 정수 범위 안에서 추가적인 문서 경계를 두지 않는다.

실제 자료량은 저장된 문자와 청크 수에만 비례해야 한다.

초기 성능 목표:

* 1,000,000개 이상의 저장 문자
* 적어도 `-1_000_000_000_000`에서 `1_000_000_000_000`까지의 양수/음수 좌표에서 입력, 저장, 렌더링
* 화면 이동 중 전체 문서 순회 금지
* 화면 밖 청크 그리기 금지

절대 픽셀 위치를 크게 계산하지 않고 카메라 기준 상대 좌표를 사용해 큰 좌표에서의 정밀도 문제를 줄인다.

---

## 25. 프로그램 구조

```text
App
│
├─ Document
│  ├─ ChunkStore
│  ├─ getCell()
│  ├─ setCell()
│  └─ deleteCell()
│
├─ Editor
│  ├─ Cursor
│  ├─ Selection
│  ├─ Clipboard
│  ├─ Bookmarks
│  ├─ Search
│  └─ History
│
├─ Camera
│
├─ Renderer
│  ├─ Text
│  ├─ Selection
│  ├─ Cursor
│  └─ Minimap
│
├─ Input
│  ├─ Keyboard
│  ├─ IME
│  ├─ Mouse
│  └─ Clipboard
│
└─ Storage
   ├─ IndexedDB
   ├─ Import
   └─ Export
```

각 계층은 다른 계층의 내부 자료구조를 직접 수정하지 않는다.

---

## 26. 초기 구현 범위

초기 버전에 다음 기능을 포함한다.

* 무한 문자 캔버스
* Canvas 2D 그리기
* 확대/이동
* 문자 입력
* 한글 입력기
* 삽입 편집
* Enter 행 분리
* 사각형 선택
* 복사/붙여넣기
* 영역 이동
* 실행 취소/다시 실행
* 문자열 찾기
* 책갈피
* 미니맵
* IndexedDB 자동 저장
* JSON 가져오기/내보내기
* TXT 내보내기

초기 버전에서 제외한다.

* WebGL/WebGPU
* DOM 문자 렌더링
* 격자 표시
* 문자별 색상/꾸미기
* 글꼴 변경
* 삽입/덮어쓰기 모드 전환
* 영역 복제
* 정규식 검색
* 플러그인
