# PixelText 사용자 경험 원칙

## 공간이 문서다

- 문서는 줄의 목록이 아니라 경계 없는 2차원 공간이다.
- 사용자는 빈 곳을 찾아 이동하고, 어디서든 바로 쓸 수 있다.
- 원점, 페이지, 격자선은 강요하지 않는다. 위치 관계 자체가 구조가 된다.

## 빈 공간도 의도다

- 문자뿐 아니라 문자 사이의 거리와 배치를 보존한다.
- 한 칸의 간격은 글의 공백으로 읽고, 넓은 간격은 서로 다른 글 덩어리를 나누는 공간으로 읽는다.
- 편집은 가까이 연결된 내용에만 영향을 주고, 떨어진 내용은 그대로 둔다.

## 익숙한 글쓰기를 공간에 맞춘다

- 입력, 줄 나누기, 지우기, 복사, 붙여넣기는 일반 문서 편집기의 기대를 따른다.
- 공간 편집에 필요한 차이만 명확히 둔다. 선택은 사각형이고, 여러 줄의 시작 위치와 빈 셀도 의미를 가진다.
- 한글 조합과 이모지를 완전한 문자 단위로 다룬다.

## 직접 조작한다

- 클릭한 곳에 쓰고, 끌어서 고르고, 고른 내용을 다시 끌어 옮긴다.
- 화면 이동과 확대는 현재 맥락을 잃지 않게 한다.
- 이동, 크기 조절, 조합 입력은 적용 전에 결과를 미리 보여 준다.

## 멀리 가도 길을 잃지 않는다

- 현재 좌표, 배율, 선택 크기를 계속 알 수 있게 한다.
- 미니맵은 전체 배치와 현재 시야의 관계를 보여 준다.
- 책갈피는 의미 있는 위치로 돌아가는 장치다.
- 검색 결과로 이동해도 해당 위치와 일치 범위를 즉시 알아볼 수 있어야 한다.

## 편집은 안전하고 되돌릴 수 있다

- 한 번의 사용자 의도는 한 번의 실행 취소 단위다.
- 여러 셀을 바꾸는 동작은 전부 성공하거나 전혀 적용되지 않는다.
- 충돌이나 한도 초과는 기존 내용을 손상시키지 않고 설명한다.
- 문서 교체 전에는 검증하고, 명시적인 새 문서 생성 전에는 손실 가능성을 확인한다.

## 저장 상태를 숨기지 않는다

- 작업은 별도 저장 명령 없이 이어서 쓸 수 있어야 한다.
- 문서와 사용자가 보던 위치를 함께 복원한다.
- 저장 대기, 진행, 완료, 실패를 구분한다. 실패를 성공처럼 보이지 않는다.
- 이식 가능한 원본 형식과 범용 텍스트 형식을 함께 제공한다.

## 캔버스에 집중시킨다

- 문자는 고정폭으로 차분하게 표시하고 격자선은 보이지 않는다.
- 도구는 가장자리에 두며, 필요한 보조 패널 하나만 연다.
- 빈 문서에서는 최소한의 시작 안내를 보이고 글이 생기면 물러난다.
- 오류와 상태는 작업을 막지 않는 위치에서 짧고 분명하게 알린다.

## 입력 방법과 화면 크기에 포용적이다

- 키보드와 포인터 양쪽에서 핵심 작업을 수행할 수 있게 한다.
- 포커스, 접근성 이름, 상태 알림, 동작 축소 설정을 존중한다.
- 좁은 화면에서는 기능을 없애기보다 도구의 글자와 부가 정보를 줄여 캔버스를 확보한다.
